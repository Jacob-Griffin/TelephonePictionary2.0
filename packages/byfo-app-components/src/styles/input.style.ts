import { css } from 'lit';

export const inputStyle = css`
  input,
  span[contentEditable] {
    box-sizing: border-box;
    height: fit-content;
    margin: 0;
    font-size: 1.4rem;
    padding: 0.25rem;
  }
`;

export default inputStyle;

import { css } from 'lit';

export const buttonStyle = css`
  button {
    height: 2rem;
    border: none;
    border-radius: 0.5rem;
    padding: 0.5rem;
    cursor: pointer;
  }
`;

export default buttonStyle;
